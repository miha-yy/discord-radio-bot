services:
  - type: web
    name: discord-radio-bot
    runtime: docker
    plan: free
    # EU region: closer to the (mostly European) radio streams and Discord
    # voice servers. Render's default region is Oregon, US.
    region: frankfurt
    healthCheckPath: /healthz
    envVars:
      - key: DISCORD_TOKEN
        sync: false
